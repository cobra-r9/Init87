title: Init87
description: "A learning phase to completely internalise the very essence till the memory of C. Not for mere job, but just to understand why C is C and why C runs the way it runs. Not a surface work anymore. I treat every piece of C code as a potential vulnerable and exploitable code. Yet, again, this is just a learning process. Projects will be in my repo. For code, you can check out the main branch."
baseurl: ""
url: "https://cobra-r9.github.io"
theme: jekyll-theme-cayman

author:
  name: Cobra Rev 9
  github: cobra-r9

highlighter: rouge
markdown: kramdown
kramdown:
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    block:
      line_numbers: false

collections:
  logs:
    output: true
    permalink: /log/:name/

defaults:
  - scope:
      path: ""
      type: "logs"
    values:
      layout: default
      
